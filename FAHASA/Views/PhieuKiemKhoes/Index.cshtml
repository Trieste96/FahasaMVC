@model IEnumerable<FAHASA.Models.PhieuKiemKho>

@{
    ViewBag.Title = "Index";
}

<h2>Danh Sách Phiếu Kiểm Kho</h2>

<p>
    @Html.ActionLink("Thêm Mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NgayGio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NgayGio)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.MaPhieu }) |
            @Html.ActionLink("Chi Tiết", "Details", new { id=item.MaPhieu }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.MaPhieu })
        </td>
    </tr>
}

</table>
