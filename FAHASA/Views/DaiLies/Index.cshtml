@model IEnumerable<FAHASA.Models.DaiLy>

@{
    ViewBag.Title = "Index";
}

<h2>Danh Mục Đại Lý</h2>

<p>
    @Html.ActionLink("Thêm Mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenDaiLy)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SoNo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenDaiLy)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SoNo)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.MaDaiLy }) |
            @Html.ActionLink("Chi Tiết", "Details", new { id=item.MaDaiLy }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.MaDaiLy })
        </td>
    </tr>
}

</table>
